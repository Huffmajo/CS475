Run the following commands to run benchmarks comparisons for array 
multiplication across 1, 4 and 8 threads. Make sure you are using bash
prior to running benchmark.

$ chmod +x benchmark
$ benchmark
